<Window x:Class="Visual_Crawl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Visual_Crawl" mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="500" WindowStartupLocation="CenterScreen" WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:DispalyValue x:Name="StaticDisplay" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Top" Panel.ZIndex="100"/>
        <local:DispalyValue x:Name="Display" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" Panel.ZIndex="100"/>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Loaded="FrameworkElement_OnLoaded">
            <Canvas x:Name="Field" Width="2000" Height="2000" HorizontalAlignment="Left"/>
        </ScrollViewer>
    </Grid>
</Window>